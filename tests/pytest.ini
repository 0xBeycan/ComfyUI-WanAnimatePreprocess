# Anchors pytest's rootdir here: the repo root is itself a package (its __init__.py does
# relative imports of ComfyUI), and rootdir at the repo would make pytest import it.
# Run as: python -m pytest tests
[pytest]
